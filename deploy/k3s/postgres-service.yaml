apiVersion: v1
kind: Service
metadata:
  name: vimpaste-postgres
  labels:
    app: vimpaste-postgres
spec:
  clusterIP: None
  selector:
    app: vimpaste-postgres
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
