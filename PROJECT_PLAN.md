# VimPaste 项目方案

> 当前阶段：仅形成方案，不进入编码、仓库初始化或公网部署。

## 1. 项目名称

**VimPaste**

- **Vim**：直接表达 Vim 键位和模态编辑体验。
- **Paste**：表达产品的核心动作——把 AI 返回的命令粘贴进来，修改后再复制走。
- 名称短、容易记，也适合作为 GitHub 仓库名、浏览器标题和未来域名的一部分。
- 一句话定位：**为 AI 生成命令准备的、支持 Vim 的临时代码编辑器。**

## 2. 要解决的问题

AI 经常返回一整条较长的 Shell、Docker、Kubernetes 或配置命令，其中夹有需要用户替换的变量，例如 `YOUR_TOKEN`、`<IP_ADDRESS>` 或 `${PASSWORD}`。直接在服务器终端里修改时存在这些困难：

1. 光标很难快速移动到命令中间。
2. 多行命令的续行符、引号和缩进容易被破坏。
3. 终端通常没有清晰的语法高亮和占位符提示。
4. 命令里可能包含 Token，不能默认上传到服务器或第三方服务。
5. 熟悉 Vim 的用户希望继续使用 `hjkl`、搜索、文本对象和普通/插入模式，而不是依赖鼠标。

VimPaste 提供一个打开即用的网页临时编辑区：粘贴、识别、高亮、定位、修改、复制，然后离开。

## 3. 产品目标与边界

### 3.1 第一版目标（MVP）

- 单页、无需登录、打开即编辑。
- 支持粘贴单行或多行代码与命令。
- 自动判断常见语言，优先正确识别 Shell 命令。
- 支持手动选择语言，自动识别错误时可以立即覆盖。
- 提供完整可用的 Vim 模式和状态栏。
- 自动发现并醒目标记常见占位符。
- 快速跳转到上一个/下一个占位符并替换。
- 一键复制编辑后的完整内容，保留换行、反斜杠和缩进。
- 默认只在浏览器内存中处理内容，不发送、不保存敏感命令。
- 可安装为 PWA，断网时也能作为临时编辑器使用。
- 以静态网站形式发布到公网。

### 3.2 暂不纳入第一版

- 用户账号、云端同步和多人协作。
- 在网页中直接执行命令或连接 SSH。
- AI 改写命令、命令安全审计或自动补全 Token。
- 在线代码仓库、文件树或完整 IDE 功能。
- 后端数据库。

这些边界能让 VimPaste 保持“安全、快速、临时”的工具属性，而不是逐渐变成另一个复杂 IDE。

## 4. 核心使用流程

1. 用户打开 VimPaste。
2. 页面自动聚焦编辑器，用户直接粘贴 AI 给出的命令。
3. 系统识别语言，显示例如 `Shell`，并应用对应语法高亮。
4. 系统识别 `YOUR_TOKEN` 等占位符，工具栏显示“1 个待替换”。
5. 用户使用 Vim 搜索和移动，也可以按 `]v` / `[v` 跳转到下一个/上一个变量占位符。
6. 用户完成替换后按复制快捷键或点击“复制”。
7. 页面提示“已复制”，用户回到服务器终端粘贴执行。
8. 刷新或关闭页面后，内容默认消失。

## 5. 页面与交互设计

第一版只需要一个编辑页面，建议采用“终端工具”风格，但保持高可读性，不模拟终端窗口的多余装饰。

### 5.1 页面结构

- **顶部工具栏**
  - VimPaste 标识。
  - 自动识别/手动选择的语言。
  - Vim 模式开关（默认开启，记住偏好但不保存编辑内容）。
  - 占位符计数和上一个/下一个按钮。
  - “复制”“清空”按钮。
- **主编辑区**
  - 占满剩余视口。
  - 行号、当前行高亮、括号匹配、搜索匹配和占位符装饰。
  - 支持长行横向滚动；不擅自换行，避免改变命令含义。
- **底部状态栏**
  - Vim 当前模式：`NORMAL`、`INSERT`、`VISUAL`、`COMMAND`。
  - 光标行列、语言名称、字符数。
  - 隐私提示：`Local only · 未上传`。
- **首次使用轻提示**
  - 仅显示一次：`粘贴命令 → ]v 跳到变量 → 修改 → 复制`。
  - 可打开快捷键帮助面板。

### 5.2 占位符识别

第一版识别规则建议覆盖：

- `YOUR_TOKEN`、`YOUR_API_KEY`、`REPLACE_ME`、`CHANGE_ME`。
- `<TOKEN>`、`<HOST>`、`<IP_ADDRESS>`。
- `${TOKEN}`、`${PASSWORD}`、`$YOUR_TOKEN`。
- 引号中的典型占位内容，例如 `'YOUR_TOKEN'`。

处理原则：

- 只做提示和跳转，绝不自动替换。
- 每个匹配项使用明显但不刺眼的背景色和下划线。
- `]v` 跳到下一个变量，`[v` 跳到上一个变量；工具栏按钮提供鼠标替代操作。
- 跳转时选中占位文本，使用户进入插入/更改操作后可直接替换。
- 允许用户忽略误判，不阻止复制。

### 5.3 Vim 支持范围

采用成熟的 CodeMirror 6 Vim 扩展，不自行重写 Vim 引擎。MVP 必须验证：

- 模式：Normal、Insert、Visual、Command-line。
- 移动：`h/j/k/l`、`w/b/e`、`0/$`、`gg/G`、`f/t`。
- 编辑：`i/a/o`、`x`、`r`、`d/c/y` 与常用文本对象。
- 搜索：`/`、`?`、`n/N`。
- 历史：`u`、`Ctrl-r`。
- 复制粘贴：内部 Vim 寄存器正常工作；网页“一键复制”始终复制整个编辑区。
- 自定义映射：`]v` 和 `[v` 用于占位符导航。

对于浏览器保留快捷键，需要在帮助页中明确说明，不能让 Vim 键位破坏浏览器的刷新、关闭标签页等系统行为。

## 6. 语言识别与高亮策略

自动识别分两层完成：

1. **场景优先规则**：若文本出现 Shebang、管道、续行反斜杠、常见 Shell 命令、环境变量赋值或 `sh -s -` 等特征，优先判为 Shell。
2. **通用检测**：使用 highlight.js 的 `highlightAuto` 在受限候选语言中评分，再映射到 CodeMirror 对应语言扩展。

候选语言不宜一次加载全部，MVP 建议支持：

- Shell/Bash
- PowerShell
- YAML
- JSON
- JavaScript/TypeScript
- Python
- SQL
- Dockerfile
- Nginx
- Plain Text

每次输入后约 300–500 ms 防抖检测，只取有限长度的文本进行判断，避免影响键入。语言包按需动态加载。用户手动选择语言后，本次内容不再自动覆盖其选择。

注意：自动识别永远存在误判，所以“当前语言”和手动选择必须清晰可见；正确、可纠正比假装完全智能更重要。

## 7. 技术方案

### 7.1 推荐技术栈

- **构建工具**：Vite。
- **语言**：TypeScript，启用严格模式。
- **UI**：React，用于管理工具栏、状态栏、提示和编辑器生命周期。
- **编辑器**：CodeMirror 6。
- **Vim 键位**：`@replit/codemirror-vim`。
- **语言检测**：highlight.js，只注册 MVP 所需语言。
- **样式**：CSS Variables + 小型自有样式系统，不引入大型 UI 组件库。
- **本地状态**：仅保存主题、Vim 开关等非敏感偏好；编辑器内容默认不写入 localStorage。
- **测试**：Vitest + Testing Library；端到端使用 Playwright。
- **代码质量**：ESLint + Prettier。
- **PWA**：Vite PWA 插件或轻量自定义 Service Worker，在功能稳定后加入。

### 7.2 为什么选择 CodeMirror 6

- 面向浏览器的可组合编辑器内核，语言、快捷键和视图能力可按需加载。
- 可用 Decoration API 标记占位符，无需修改用户原始文本。
- 官方扩展体系包含搜索、历史、括号匹配、语言支持等能力。
- `@replit/codemirror-vim` 明确面向 CodeMirror 6，能覆盖本项目最重要的 Vim 体验。
- 相比完整 Monaco Editor，首屏体积和定制成本更适合“单缓冲区临时编辑器”。

参考：

- [CodeMirror 系统指南](https://codemirror.net/docs/guide/)
- [CodeMirror 扩展列表](https://codemirror.com/docs/extensions/)
- [CodeMirror Vim 扩展](https://github.com/replit/codemirror-vim)
- [highlight.js 官网](https://highlightjs.org/)

### 7.3 建议的未来目录结构

```text
VimPaste/
├── docs/                    # 产品、架构、隐私与快捷键文档
├── public/                  # 图标、manifest 等静态资源
├── src/
│   ├── components/         # 工具栏、状态栏、帮助面板
│   ├── editor/             # CodeMirror 配置、Vim 映射
│   ├── detection/          # 语言与占位符识别
│   ├── storage/            # 非敏感偏好存储
│   ├── styles/             # 主题与基础样式
│   ├── App.tsx
│   └── main.tsx
├── tests/
├── index.html
├── package.json
└── vite.config.ts
```

这只是实施阶段的目录建议；当前阶段不创建以上代码结构。

## 8. 隐私与安全设计

命令中很可能包含真实 Token、密码、IP 和内网地址，因此隐私是产品功能，而不是附加说明。

- 核心编辑、识别和高亮全部在浏览器端完成。
- 第一版不提供后端接口，不上传编辑内容。
- 编辑内容默认不持久化；刷新页面即清除。
- 不将编辑内容写入 URL、日志、分析事件或错误上报。
- 若未来加入崩溃报告，必须过滤编辑器内容和剪贴板内容。
- 复制只在用户主动操作时调用 Clipboard API。
- 清空操作需要立即生效；若内容非空，可提供一次轻量确认。
- 页面显示固定的“Local only”状态，隐私说明页明确解释技术边界。
- 公网部署设置严格 CSP，限制脚本、连接和资源来源。

## 9. 公网发布方案

VimPaste 是纯静态应用，优先采用 GitHub 仓库连接 Cloudflare Pages：

1. 创建公开或私有 GitHub 仓库 `vimpaste`。
2. `main` 分支作为生产环境，Pull Request 生成预览环境。
3. 构建命令为 `npm run build`，输出目录为 `dist`。
4. 首次获得 `*.pages.dev` 地址后再决定是否购买独立域名。
5. 配置 HTTPS、安全响应头、SPA 路由回退和缓存策略。
6. 发布后用真实桌面/移动浏览器验证剪贴板、Vim 输入法与 PWA。

Cloudflare 当前仍支持静态站点、Git 集成和预览部署；同时其文档已提示 Workers 是新项目的主平台。因此实施阶段应再次确认：若 Pages 的创建入口或产品策略变化，则保持相同静态构建产物，切换到 Cloudflare Workers Static Assets，不改变应用架构。

参考：[Cloudflare Pages 文档](https://developers.cloudflare.com/pages/)、[静态站点部署指南](https://developers.cloudflare.com/pages/framework-guides/deploy-anything/)

## 10. 实施阶段划分

### 阶段 A：编辑器骨架

- 初始化 Vite + React + TypeScript。
- 接入 CodeMirror 6 和 Shell 高亮。
- 完成全屏编辑、复制、清空、状态栏。
- 接入 Vim 模式并验证核心命令。

### 阶段 B：智能识别

- 实现 Shell 优先的语言检测。
- 接入通用自动检测和手动语言覆盖。
- 实现占位符识别、装饰、计数及 `[v` / `]v` 导航。

### 阶段 C：质量与隐私

- 单元测试覆盖语言和占位符规则。
- 端到端覆盖粘贴、Vim 替换和复制。
- 检查没有网络请求或内容持久化。
- 完成桌面、移动、中文输入法和可访问性测试。

### 阶段 D：公网发布

- 加入 PWA 与离线缓存。
- 配置 GitHub CI。
- 部署 Cloudflare Pages 或 Workers Static Assets。
- 配置域名、安全头、隐私说明和发布检查。

## 11. MVP 验收标准

使用以下命令作为首要验收样例：

```bash
curl -sfL https://get.k3s.io | K3S_TOKEN='YOUR_TOKEN' sh -s - server \
  --server https://10.10.0.11:6443 \
  --node-ip 10.10.0.12 \
  --advertise-address 10.10.0.12 \
  --flannel-iface eth1
```

验收时必须证明：

1. 粘贴后自动识别为 Shell/Bash 并产生正确语法高亮。
2. `YOUR_TOKEN` 被识别为占位符并明显标记。
3. 用户可以用 `]v` 定位到该占位符，然后用 Vim 操作完成替换。
4. 多行结构、缩进、单引号和每一处反斜杠都保持不变。
5. “复制”得到的文本与编辑器最终内容完全一致。
6. 整个操作过程中，编辑内容没有发送到网络，也没有默认持久化。
7. 刷新页面后敏感内容消失，Vim 偏好可保留。
8. 公网 HTTPS 地址可访问，离线重新打开已缓存应用仍可使用核心编辑功能。

## 12. 风险与应对

| 风险                           | 影响             | 应对方式                                           |
| ------------------------------ | ---------------- | -------------------------------------------------- |
| 自动语言识别误判               | 高亮不正确       | Shell 优先规则、限制候选集、允许手动覆盖           |
| Vim 插件与浏览器快捷键冲突     | 操作意外         | 建立快捷键冲突清单并做端到端测试                   |
| Token 被本地存储或分析工具采集 | 严重隐私问题     | 默认内存态、不做内容埋点、网络请求测试、严格 CSP   |
| 多行命令被自动换行或格式化     | 命令执行失败     | 不做自动格式化、不改变文本、复制一致性测试         |
| 移动端 Vim 输入体验差          | 可用性下降       | 移动端可关闭 Vim，保留触控与工具栏导航             |
| PWA 缓存旧版本                 | 修复不能及时生效 | 版本化缓存、更新提示、只缓存应用资源不缓存编辑内容 |

## 13. 后续可选增强

在 MVP 验收完成后再考虑：

- 多个临时缓冲区，但仍默认不持久化。
- 用户自定义占位符规则和 Vim 映射。
- 命令差异对比和“修改了哪些位置”提示。
- 仅本地运行的危险命令提示规则。
- 分享链接时只分享模板、不包含填入的秘密值。
- 桌面端快捷入口、浏览器扩展或系统级快速粘贴窗口。

## 14. 本阶段交付结论

VimPaste 第一版应被实现为一个无后端、隐私优先的静态单页应用。核心技术路线是 React + TypeScript + CodeMirror 6 + CodeMirror Vim + 受限的自动语言检测，并以提供的 K3s 命令完成端到端验收。当前阶段只保存本方案，不创建项目代码、不初始化 Git，也不执行公网部署。
